export const colors = {
    linkColor: "#2F80ED",
    textColor: "#212121",
    whiteColor: "#FAFAFA"
};
export const setUpBotBorder = (width:number, type:string, color:string) => {
    return `${width}px ${type} ${color}`;
};

export const LightTheme = {
    background: "#fff"
};
export const DarkTheme = {
    background: "#F5F5F5"
};